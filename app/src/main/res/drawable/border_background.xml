<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Capa 1: Fondo con color diferente -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/fondo_info" />
            <corners android:radius="35dp" /> <!-- Radio para bordes redondos, ajusta el valor según tu preferencia -->
        </shape>
    </item>

    <!-- Capa 2: Borde con color y ancho personalizados -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="10dp"
                android:color="@color/colorPrimaryDark"
                />
            <solid android:color="#00000000" />
            <corners android:radius="30dp"></corners>
        </shape>
    </item>

</layer-list>
